<Window x:Class="batteryQI.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:batteryQI.Views"
        xmlns:vm="clr-namespace:batteryQI.ViewModels.Bases"
        Title="LoginWindow" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- 로그인 UI -->
        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
            <TextBox Text="{Binding Manager.ManagerID, UpdateSourceTrigger=PropertyChanged}" x:Name="UsernameBox" Width="200" Margin="5"/>
            <PasswordBox x:Name="PasswordBox" Width="200" Margin="5"/>
            <Button Content="Login" Width="200" Margin="5" Command = "{Binding LoginCommand}" CommandParameter="{Binding ElementName=PasswordBox}"/>
        </StackPanel>
    </Grid>
</Window>
